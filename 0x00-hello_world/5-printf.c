#include <stdio.h>
/*
 * main - Using printf to print exactly with proper grammar, but the outcome is a piece of art,
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
